{
  "name": "my-cospace",
  "version": "0.1.0",
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "build": "lage build",
    "build:clean": "lage build --no-cache",
    "clean": "lage clean && rimraf node_modules",
    "setOverrides": "node ./scripts/setOverrides.js"
  },
  "devDependencies": {
    "lage": "latest",
    "rimraf": "^3.0.2"
  },
  "pnpm": {
    "overrides": {},
    "prefer-workspace-packages": true,
    "link-workspace-packages": "deep"
  }
}
